# 무료 공개 데이터 기반 개인 투자자용 퀀트 파이프라인의 방법론적 취약성 분석: 결함별 기여도 분해

> **초고 (draft)** · 작성일 2026-09-18
> 채워 넣어야 할 항목: 저자·소속·지도교수·영문 초록·제출 형식(학회 템플릿에 맞춘 재배치)
> 모든 실험 결과 반영 완료

---

## 초록

주가 수익률 예측 알고리즘을 제안하는 연구는 대단히 많지만, 그중 상당수는 재현되지 않는다. 본 연구는 새로운 예측 알고리즘을 제안하는 대신, **널리 쓰이는 무료 데이터(yfinance)와 개인 투자자 수준의 구현으로 만든 실제 작동하는 퀀트 파이프라인을 대상으로, 방법론적 결함이 각각 얼마만큼의 '없는 예측력'을 만들어내는지를 정량적으로 분해**한다.

동일한 데이터와 동일한 모델에 대해 결함을 하나씩 켜고 끄는 절제 실험(one-at-a-time ablation)을 수행한 결과, 결함을 모두 포함한 설정에서는 AUC 0.587, p=0.043으로 **"통계적으로 유의한 예측력"이 관측**되었다. 그러나 결함을 교정해 나가면 유의성이 소멸하며, **모든 결함을 교정한 최종 추정치는 AUC 0.481, p=0.931로 무작위 수준(0.5) 아래**이다.

기여도 분해 결과 다음이 확인되었다. 첫째, 지수 편입일을 무시하고 현재 구성종목을 전 기간에 적용하는 관행은 AUC를 **0.030 부풀렸으며**(0.484 → 0.514), 이는 측정된 단일 결함 중 최대였다. 둘째, 횡단면 패널의 종속성을 무시하고 관측치 수를 표본 수로 사용하는 유의성 검정은, 참효과가 0인 합성 데이터에서 유의수준 5%에 대해 **35.5%의 오탐률**을 보였다(1,000회 반복, 95% CI [32.5%, 38.6%]). 셋째, 개인 프로젝트의 전형적 규모인 42개 종목 유니버스에서는 **무작위로 구성한 유니버스의 46%가 p<0.05를 산출**하였다. 넷째, 60개의 합리적인 분석 설정을 시도한 뒤 최선만 보고할 경우 p값은 0.161에서 0.0115로 변화하였다.

또한 결함의 효과는 **가산적이지 않았다.** 개별 효과를 단순 합산하면 음수이지만 동시에 적용하면 양의 큰 값이 되며, 이는 일부 결함이 추정치를 끌어올리고 다른 결함이 그 추정치에 유의성을 부여하는 순차적 상호작용에서 비롯된다.

한편 사전에 심각하리라 예상했던 일부 결함(연간 재무제표의 공시 지연 오설정, 이익성장률 부호 오류, 종목 단위 과적합)은 본 설정에서 유의한 영향을 보이지 않았다. 이는 결함의 논리적 심각성과 실제 성능 왜곡 정도가 일치하지 않음을 보여준다.

마지막으로 본 데이터의 검정력은 약 25%로 추정되었다. 따라서 본 연구의 결론은 "예측력이 없음을 증명했다"가 아니라 **"관측되던 미약한 신호는 결함의 산물이었으며, 현재의 데이터 규모로는 실제 신호의 존재 여부를 판별할 수 없다"**이다.

**주제어**: 백테스트 과적합, 다중검정, 생존 편향, Fama-MacBeth 검정, 재현성, 퀀트 투자

---

## Abstract

Studies proposing stock return prediction algorithms are abundant, yet many fail to replicate. Rather than proposing another predictive algorithm, this study takes a working quantitative investment pipeline—built with widely used free data (yfinance) and an implementation typical of retail investors—and **quantitatively decomposes how much spurious predictive power each methodological defect generates**.

Applying one-at-a-time ablation to an identical dataset and model, the configuration containing all defects yielded AUC 0.587 with p=0.043, an apparently **"statistically significant" predictive ability**. As defects are corrected the significance disappears, and the **fully corrected estimate is AUC 0.481 with p=0.931—below the random benchmark of 0.5**.

The decomposition yields four principal findings. First, ignoring index addition dates and applying the current constituent list to the entire sample period inflated AUC by **0.030** (0.484 → 0.514), the largest single-defect effect measured. Second, a significance test that disregards cross-sectional dependence and treats observation counts as sample size produced a **35.5% false positive rate** at a nominal 5% level on synthetic data with zero true effect (1,000 replications, 95% CI [32.5%, 38.6%]). Third, at 42 stocks—a universe size typical of retail projects—**46% of randomly constructed universes yielded p<0.05**. Fourth, trying 60 defensible analysis configurations and reporting only the best shifted the p-value from 0.161 to 0.0115.

Defect effects were also **non-additive**: individual effects sum to a negative value while their joint application produces a large positive one, arising from a sequential interaction in which some defects inflate the estimate and others confer significance upon it.

Conversely, several defects expected to be severe—misspecified reporting lag for annual statements, a sign error in earnings growth, and stock-level overfitting—showed no significant effect in this setting, demonstrating that a defect's logical severity need not match its empirical distortion.

Finally, the statistical power of this dataset is estimated at approximately 25%. The conclusion is therefore not that predictive ability has been disproven, but rather that **the weak signal previously observed was an artifact of methodological defects, and the present data volume cannot determine whether a genuine signal exists.**

**Keywords**: backtest overfitting, multiple testing, survivorship bias, Fama-MacBeth regression, reproducibility, quantitative investing

---

## 1. 서론

### 1.1 연구 배경

주가 예측 알고리즘을 제안하는 연구와 공개 프로젝트는 매우 많다. 그러나 Hou, Xue, and Zhang(2020)은 학술지에 발표된 이상현상(anomaly)의 상당수가 재현되지 않음을 보였고, Harvey, Liu, and Zhu(2016)는 다중검정을 고려하면 기존에 보고된 팩터 대부분이 거짓 발견일 수 있다고 주장하였다. Bailey et al.(2014)은 백테스트 과적합이 어떻게 무의미한 전략을 우수해 보이게 만드는지를 이론적으로 분석하였다.

이러한 연구들은 대체로 **기관·학계 수준의 연구 환경**(유료 데이터, 전문 연구자)을 전제한다. 그러나 오늘날 주가 예측 시도의 상당수는 다른 곳에서 이루어진다. 무료 데이터 라이브러리(yfinance 등)와 오픈소스 머신러닝 도구, 그리고 간단한 웹 대시보드로 구성된 **개인 투자자 수준의 파이프라인**이 그것이다. 이 영역의 방법론적 건전성은 체계적으로 감사된 바가 없다.

### 1.2 문제 제기

본 연구의 출발점은 저자가 직접 구축한 가치투자 랭킹 시스템이었다. 이 시스템은 재무 지표를 입력으로 받아 6개월 뒤 횡단면 상대 성과를 예측하며, 초기 구현에서 **AUC 0.59, p=0.041**이라는 "통계적으로 유의한" 결과를 산출하였다.

그러나 종목 수를 41개에서 501개로 확대하자 이 유의성은 사라졌다. 이 관찰은 다음 질문으로 이어진다.

> 그 유의성은 시장에서 발견한 신호였는가, 아니면 방법론이 만들어낸 산물이었는가?
> 만약 후자라면, 어떤 결함이 얼마나 기여했는가?

### 1.3 본 연구의 기여

본 연구는 새로운 예측 알고리즘을 제안하지 않는다. 대신 다음 세 가지를 기여한다.

1. **대상의 신규성** — 기존 문헌이 다루지 않은 *무료 데이터 기반 개인 투자자 스택*을 감사 대상으로 삼는다.
2. **방법의 신규성** — 이론적·시뮬레이션 분석이 아니라, **실제 작동하는 시스템에서 결함을 하나씩 켜고 끄는 절제 실험**으로 각 결함의 기여도를 분리 측정한다.
3. **음성 결과의 정직한 보고** — 예상과 달리 영향이 없었던 결함들, 그리고 저자가 세웠다가 데이터로 기각된 가설을 함께 보고한다.

---

## 2. 데이터 및 시스템

### 2.1 데이터

| 항목 | 내용 |
|---|---|
| 가격 데이터 | yfinance (일별 수정주가), 최근 5년 |
| 재무 데이터 | yfinance 연간·분기 재무제표 (손익계산서·재무상태표) |
| 유니버스 | S&P500 구성종목 494개 + 개별 관심종목 7개 = 501개 |
| 시점별 구성종목 | 공개 데이터셋(1996–2026, 스냅샷 2,720개) |
| 거시 변수 | VIX, 미국 10년물 금리, S&P500, 유가, 금, 달러지수 |

### 2.2 예측 문제 정의

- **대상**: 각 종목의 *h*일 후 수익률이 같은 날짜 전 종목의 중앙값을 초과하는가 (이진 분류)
- **기본 예측 기간**: *h* = 126 거래일(약 6개월)
- **모델**: RandomForest (트리 120개, 최대 깊이 3, 최소 잎 크기 10)
- **입력 변수 9종**: 이익수익률(E/P), 순자산비율(B/P), ROE, 순이익률, 이익성장률, PEG 역수, 6개월 모멘텀, 60일 변동성, 시장 대비 상대강도
- **전처리**: 각 날짜 내에서 변수를 백분위 순위화(0~1), 결측은 중립값 0.5로 대체

PER·PBR 대신 그 역수를 사용한 이유는, PER이 적자 기업에서 음수가 되어 '저평가'와 순서가 역전되고 이익이 0에 근접하면 발산하기 때문이다.

### 2.3 평가 설계

- 날짜 기준 확장 윈도우 교차검증(3-fold)
- 학습 구간과 평가 구간 사이에 *h*일의 퍼징(purging) 간격을 두어 라벨 누출 차단
- 성능 지표: AUC
- 유의성 검정: Fama-MacBeth 방식 + Newey-West 보정 (3.1절 참조)

---

## 3. 결함 분류 체계

본 연구가 측정 대상으로 삼은 결함은 다음 8가지이다. 각 결함은 개인 퀀트 프로젝트에서 실제로 흔히 관찰되는 것들이다.

### 3.1 ① 횡단면 종속성을 무시한 유의성 검정

AUC의 표준오차를 구하는 Hanley-McNeil 공식은 **관측치가 서로 독립**임을 가정한다. 그러나 횡단면 패널의 행(종목×날짜)은 독립이 아니다.

1. 같은 날짜의 종목들은 같은 시장 환경을 공유한다.
2. 라벨이 '그날 중앙값 초과'로 정의되므로, 날짜마다 정확히 절반이 양성으로 **강제**된다.
3. 같은 종목이 여러 평가일에 반복 등장한다.

따라서 실제 독립 단위는 행이 아니라 **평가 날짜**이다. 본 연구는 횡단면 자산가격 연구의 표준인 **Fama-MacBeth(1973) 방식**을 사용한다. 즉 날짜별로 AUC를 따로 구한 뒤, 그 시계열의 평균이 0.5와 다른지를 t검정한다(자유도 = 날짜 수 − 1).

나아가 예측 기간이 *h*일이면 인접한 평가일의 라벨은 *h*−1일치가 겹치므로 날짜별 통계량이 자기상관된다. 이를 **Newey-West(1987) 보정**(Bartlett 커널, lag = *h*)으로 처리한다.

### 3.2 ② 소표본 유니버스

개인 프로젝트는 보통 관심 종목 수십 개로 시작한다. 본 연구의 초기 구현도 41개였다.

### 3.3 ③ 연간 재무제표의 공시 지연 오설정

결산일 당일에는 아무도 그 실적을 모른다. 따라서 재무 데이터는 실제 공시 이후 시점부터 사용해야 한다. 초기 구현은 분기·연간에 동일한 지연(45일)을 적용했으나, SEC 제출 기한은 서로 다르다.

- 10-Q(분기): 40~45일
- 10-K(연간): 60~90일

따라서 연간 재무제표에 45일을 적용하면 **실제 공시 전의 정보를 사용**하게 된다. 본 연구는 분기 45일 / 연간 90일로 분리하였다.

> **방법론적 주의**: 이 상수는 화면에 표시되는 최신 수치가 상용 증권 앱과 일치하는지를 보고 조정해서는 안 된다. 그것은 제품 편의를 위해 룩어헤드 방지를 훼손하는 행위이다. 본 연구는 SEC 제출 기한으로부터 사전에(a priori) 값을 결정하였다.

### 3.4 ④ 이익성장률의 부호 오류

성장률을 `pct_change`(= (신규−직전)/직전)로 계산하면, 직전 순이익이 음수일 때 부호가 뒤집힌다.

| 실제 상황 | 계산 결과 | 문제 |
|---|---|---|
| 적자 −200 → 흑자 +20 (개선) | **−110%** | 최악으로 기록 |
| 적자 −50 → 적자 −200 (악화) | **+300%** | 고성장으로 기록 |

즉 모델에게 "적자가 커지는 기업이 고성장"이라고 학습시키게 된다. 분모를 절댓값으로 바꾸면 모든 경우에 방향이 일치하며, 흑자 구간의 동작은 기존과 동일하다.

### 3.5 ⑤ 평가일 솎아내기로 인한 검정력 상실

겹치는 라벨을 피하려면 평가일을 *h*일 간격으로 솎아내는 방법이 있다. 통계적으로는 정직하지만 **가용 데이터의 1/h만 사용**하게 된다. 본 연구의 초기 구현에서 이 방식은 독립 평가일을 **3개**로 축소시켰다.

### 3.6 ⑥ 다중검정 (researcher degrees of freedom)

연구자는 설정 하나만 시도하고 끝내지 않는다. 변수 조합, 모델 초모수, 예측 기간을 바꿔 보고 **가장 좋은 결과를 보고**한다. 이때 보고되는 p값은 더 이상 명목 유의수준을 의미하지 않는다.

### 3.7 ⑦ 종목 단위 과적합 (가설)

날짜로만 교차검증을 나누면 같은 종목이 학습·평가에 모두 등장한다. 따라서 모델이 일반화 가능한 팩터 관계 대신 **"이 종목은 대체로 평균을 이긴다"는 종목별 습성**을 기억할 가능성이 있다. 본 연구는 이를 가설로 세우고 검증하였다.

### 3.8 ⑧ 생존 편향 및 지수 편입일 룩어헤드

현재 시점의 지수 구성종목으로 과거를 분석하면 두 가지 문제가 동시에 발생한다.

- **편입일 룩어헤드**: 현재 구성종목 503개 중 79개(15.7%)는 최근 5년 내 편입되었다. 이들을 전 기간에 포함하면 "이 기업이 훗날 지수에 편입될 것"을 미리 아는 셈이며, 지수 편입은 통상 양호한 성과의 결과이므로 체계적 상방 편향이 된다.
- **생존 편향**: 해당 기간에 지수에서 탈락한 종목이 표본에서 누락된다. 분석 기간 중 한 번이라도 편입된 종목 615개 가운데 114개가 현재는 구성종목이 아니다.

이 결함은 유니버스 구성 자체를 변경하므로 4.1절의 절제 실험과 별도로 측정하였다(4.5절). 결과적으로 **본 연구에서 측정된 단일 결함 중 최대 효과**를 보였다.

---

## 4. 실험 결과

### 4.1 주 결과: 결함별 기여도 분해

동일한 데이터·모델에 대해 결함을 하나씩만 켠 결과는 다음과 같다. 각 행은 해당 결함 하나만 활성화하고 나머지는 모두 교정한 상태이다.

**[표 1] 결함별 단독 효과**
*주: 본 표의 기준선은 ⑧(생존편향·지수 편입일 룩어헤드)이 **교정되지 않은** 상태이다. ⑧은 유니버스 구성 자체를 변경하므로 별도 실험으로 측정하였다(4.5절). ⑧까지 교정한 최종 기준선은 AUC 0.481이다.*

| 설정 | 종목 | AUC | t | p | 평가일 | 판정 |
|---|---:|---:|---:|---:|---:|---|
| 기준선 (⑧ 제외 전부 교정) | 501 | 0.514 | +0.99 | 0.161 | 299 | 유의하지 않음 |
| **① iid 가정 p값** | 501 | 0.514 | +0.99 | **0.0000** | 299 | **유의** |
| **② 소표본 유니버스** | 42 | 0.538 | +2.09 | **0.0188** | 297 | **유의** |
| ③ 연간 룩어헤드 | 501 | 0.510 | +0.68 | 0.249 | 322 | 유의하지 않음 |
| ④ 성장률 부호 오류 | 501 | 0.518 | +1.13 | 0.130 | 299 | 유의하지 않음 |
| ⑤ 평가일 솎아내기 | 501 | 0.477 | −2.18 | 0.919 | **3** | 유의하지 않음 |
| **모든 결함 포함 (초기 구현)** | 42 | **0.587** | +1.30 | **0.0429** | 3 | **유의** |

가장 중요한 관찰은 **맨 아래 행**이다. 결함을 모두 포함한 설정은 초기 구현에서 관측된 값(AUC 0.59, p=0.041)을 재현하며, 결함을 제거하면 유의성이 소멸한다. 즉 그 유의성은 시장 신호가 아니라 방법론의 산물이었다.

⑧의 결과는 4.5절에서 다룬다. 결론을 앞당겨 말하면 **⑧은 본 연구에서 측정된 단일 결함 중 가장 큰 효과를 보였다.**

### 4.2 ①의 단독 효과: 동일한 성능, 상반된 결론

**①은 가상의 설정이 아니라 본 시스템의 초기 구현이 실제로 사용하던 방식이다.** 초기 구현은 통합한 예측 결과 전체에 대해 AUC를 구한 뒤, Hanley-McNeil 공식에 `n_pos`/`n_neg`로 **행(종목×날짜)의 개수를 그대로 투입**하였다. 이는 개인 퀀트 프로젝트에서 흔히 관찰되는 처리이기도 하다. 따라서 ①은 "같은 설정을 두 방식으로 검정해 본 것"이 아니라, **실제로 사용되던 검정을 올바른 검정으로 교체한 것**이다.

기준선과 ①은 **AUC(0.514)와 t값(+0.99)이 완전히 동일**하다. 데이터도 모델도 예측 결과도 같다. 바뀐 것은 p값 계산식 하나뿐인데 **p가 0.161에서 0.0000으로 변화**한다.

AUC가 변하지 않는다는 사실은 이 결함의 성격을 정확히 드러낸다. **①은 추정(estimation)이 아니라 추론(inference)에만 작용한다.** 즉 모델의 실제 성능은 전혀 개선하지 않으면서, 그 성능에 대한 신뢰도 진술만 왜곡한다.

이 결함의 심각성을 독립적으로 확인하기 위해, 참예측력이 0인 합성 패널을 **1,000회** 생성하여 두 검정을 비교하였다. 구간은 Clopper-Pearson 정확 95% 신뢰구간이다.

**[표 2] 참효과가 0일 때의 오탐률 (명목 유의수준 5%, 1,000회 반복)**

*사례 1: 횡단면 종속성 — 평가일 3개 × 종목 300개*

| 검정 방식 | 오탐률 | 95% CI |
|---|---:|---|
| 행 수를 표본 수로 사용 (기존) | **35.5%** | [32.5%, 38.6%] |
| Fama-MacBeth | **5.3%** | [4.0%, 6.9%] |

두 방식의 차이는 Fisher 정확검정에서 p = 1.9×10⁻⁶⁸로, 표본 변동으로 설명되지 않는다. Fama-MacBeth의 오탐률 신뢰구간은 명목 수준 5%를 포함한다.

*사례 2: 겹치는 예측 구간 — 평가일 260개, 예측기간 20일*

| 검정 방식 | 오탐률 | 95% CI |
|---|---:|---|
| 겹침 무시 (iid) | **33.9%** | [31.0%, 36.9%] |
| Newey-West 보정 | **10.0%** | [8.2%, 12.0%] |

차이는 Fisher 정확검정에서 p = 2.0×10⁻³⁹이다.

> **주목할 점**: Newey-West 보정은 오탐률을 33.9%에서 10.0%로 크게 낮추지만, **명목 수준 5%까지 회복시키지는 못한다**(신뢰구간 [8.2%, 12.0%]가 5%를 포함하지 않는다). 즉 겹치는 예측 구간에서는 표준 보정을 적용하더라도 유의성 판정이 여전히 약 2배 관대하다. 이는 6장의 '검정의 낙관성' 한계를 실측으로 뒷받침한다.

### 4.3 ②의 단독 효과: 소표본에서는 무엇을 골라도 유의해진다

초기 구현의 42개 종목 유니버스가 p=0.0188을 산출한 원인이 (A) 소표본 자체인지 (B) 저자의 종목 선택 편향인지를 구분하기 위해, S&P500에서 **무작위로 42개 종목을 100회 추출**하여 동일한(모든 결함이 교정된) 파이프라인을 반복 적용하였다.

| 항목 | 값 |
|---|---:|
| p<0.05 비율 | **46%** (95% CI [36.1%, 56.1%], n=100) |
| p<0.10 비율 | 55% |
| AUC 중앙값 | 0.528 (5–95% 구간 0.473–0.594) |
| 손으로 고른 유니버스의 위치 | 분포의 **46 백분위** |

명목 오탐률 5%는 이 신뢰구간에 포함되지 않으므로, 관측된 과대 검출은 표본 변동으로 설명되지 않는다.

두 가지가 드러난다. 첫째, **무작위로 종목을 뽑아도 절반 가까이가 "유의"하게 나온다**(명목 오탐률의 9배). 둘째, 저자가 손으로 고른 명단은 무작위 분포의 한가운데에 있어 **특별히 편향된 선택이 아니었다**. 즉 문제는 선택의 주관성이 아니라 **소표본 자체**이며, 이는 더 일반적인 결론이다.

### 4.4 ⑥의 효과: 무엇을 보고할지 고르는 것만으로 결론이 바뀐다

연구자가 실제로 조정할 법한 손잡이 세 종류(변수 조합 5가지 × 예측 기간 3가지 × 모델 초모수 4가지 = **60개 설정**)를 모두 시도하였다. 데이터와 파이프라인은 전부 동일하다.

| 보고 방식 | AUC | p | 결론 |
|---|---:|---:|---|
| 사전에 정한 설정 하나 | 0.514 | 0.161 | 유의하지 않음 |
| 60개 중 최선만 보고 | 0.519 | **0.0115** | **유의** |

60개 중 13개(22%)가 p<0.05를 산출하였다. 그런데 **그 13개의 분포는 무작위가 아니었다.**

| 조건 | p<0.05 개수 |
|---|---:|
| 예측 기간 **1개월** | **13 / 20** |
| 예측 기간 3개월·6개월 | **0 / 40** |

유의성은 전적으로 1개월 설정에서만 발생하며, 모델 초모수(깊이 2/3/5, 잎 크기 20)는 거의 영향이 없었다. 또한 1개월 설정 안에서도 **변동성 변수를 제거하면 유의성이 사라진다**(p=0.142). 반면 재무 변수(PEG, 이익성장률)는 제거해도 유의성이 유지된다.

이는 중요한 함의를 갖는다. **"가치투자 모델"이라 명명된 이 파이프라인이 실제로 포착하는 것은 재무 지표가 아니라 단기 가격 변동성이다.** 변수 중요도 분석도 이를 뒷받침한다.

| 변수 | 중요도 | | 변수 | 중요도 |
|---|---:|---|---|---:|
| ROE | 21.3% | | 이익성장률 | 8.6% |
| **60일 변동성** | **17.2%** | | 순이익률 | 7.8% |
| 이익수익률(E/P) | 14.5% | | 시장 대비 상대강도 | 5.3% |
| 순자산비율(B/P) | 11.8% | | PEG 역수 | 2.2% |
| 6개월 모멘텀 | 11.1% | | | |

가격 기반 변수(변동성·모멘텀·상대강도)가 전체 중요도의 **33.6%**를 차지한다.

### 4.5 ⑧의 효과: 단일 결함 중 최대

지수 편입일과 시점별 구성종목을 반영하기 위해, 공개 시점별 구성종목 데이터(1996–2026, 스냅샷 2,720개)를 사용하여 세 조건을 비교하였다. 분석 기간 중 한 번이라도 지수에 편입된 종목은 615개였고, 그중 현재 구성종목이 아닌 종목은 114개였다.

| 조건 | 편입일 | 탈락 종목 | AUC | t | p |
|---|---|---|---:|---:|---:|
| A. 현재 방식 | 무시 | 없음 | 0.514 | +0.99 | 0.161 |
| B. 편입일 반영 | 반영 | 없음 | 0.484 | −1.02 | 0.845 |
| C. 시점별 구성종목 | 반영 | 복원 | **0.481** | −1.49 | 0.931 |

기여도 분해는 다음과 같다.

- **편입일 룩어헤드**: AUC 0.514 → 0.484 (**−0.030**)
- **생존 편향(부분 교정)**: AUC 0.484 → 0.481 (−0.002)

편입일 반영만으로 전체 행의 10.0%가 제거되었고(649,296 → 584,666행), 탈락 종목 복원으로 41,135행이 추가되었다.

**편입일 룩어헤드 단독 효과(+0.030)는 본 연구에서 측정된 모든 단일 결함 중 가장 크다**(②소표본 +0.024보다 크다). 원인은 명확하다. 지수 편입은 통상 양호한 성과의 *결과*이므로, 편입 이전 기간까지 해당 종목을 포함시키면 "훗날 우수한 성과를 낼 기업"을 미리 아는 것과 같다.

더 중요한 함의가 있다. **⑧을 교정하면 AUC 점추정치가 0.481로 0.5 아래로 내려간다.** 즉 4.1절 기준선에서 관측되었던 미미한 양(+)의 점추정치(0.514)조차 상당 부분 이 결함의 산물이었다.

생존 편향의 추가 효과(−0.002)가 작게 나타난 것은 교정이 부분적이기 때문일 수 있다. 탈락 종목 114개 중 **62개(54%)만 복원**되었으며, 나머지는 인수·합병 또는 사명 변경으로 조회되지 않는다. 그리고 그 결측은 무작위가 아니다(6장 참조).

### 4.6 영향이 없었던 결함들 (음성 결과)

**③ 연간 룩어헤드**는 AUC를 0.514에서 0.510으로 오히려 소폭 낮추었다. 이는 초기의 저검정력 측정(평가일 3개)에서 관측된 큰 효과가 노이즈였음을 시사한다.

**④ 성장률 부호 오류**는 AUC를 0.514에서 0.518로 변화시켰으나 유의하지 않았다. 원인을 추적한 결과, 이 결함이 값을 변경하는 행은 전체의 **3.85%**에 불과했다(120종목 표본 기준, 부호가 뒤집힌 종목은 20개). S&P500 대형주는 대부분 흑자여서 '직전 순이익이 음수'인 경우가 드물기 때문이다. 해당 변수의 중요도가 8.6%임을 고려하면 총 영향은 약 0.33%에 그친다.

> 이는 **결함의 논리적 심각성과 실제 왜곡 정도가 일치하지 않음**을 보여준다. 동일한 결함이라도 적자 기업 비중이 높은 유니버스(소형주·바이오텍·성장주)에서는 영향이 훨씬 클 것으로 예상된다.

**⑦ 종목 단위 과적합**은 가설이 기각되었다. 유니버스를 무작위로 절반(S1, S2)으로 나눈 뒤, **학습은 S1으로 고정하고 평가 종목만 교체**하는 통제 실험을 20회 반복하였다.

| 평가 대상 | AUC | p<0.05 비율 |
|---|---:|---:|
| 학습에서 본 종목(S1) | 0.514 | 25% |
| 학습에 없던 종목(S2) | 0.511 | 20% |
| 차이 | **+0.002** | — |

대응표본 t검정 결과 t=+0.62, p=0.543으로 차이가 유의하지 않았다. 모델이 종목별 습성을 기억한다는 증거는 발견되지 않았다.

### 4.7 검정력 분석: 관측된 유의 비율의 정합성

4.6절의 종목 분할 실험에서 **250종목 규모에서도 20~25%가 p<0.05를 산출**했다는 점은 별도의 해석을 요구한다. 이 실험은 ⑧이 교정되지 않은 패널을 사용했으며, 해당 조건의 AUC는 0.514, t=+0.99였다. 즉 **효과 크기가 표준오차의 약 1배**이다. 참효과가 1 표준오차일 때 단측 5% 유의수준에서의 검정력은

```
P(Z > 1.645 − 1) = P(Z > 0.645) ≈ 26%
```

이며, 이는 관측된 20~25%와 일치한다. 다시 말해 그 조건에서 관측된 유의 사례들은 **⑧이 만들어낸 약 1 표준오차 크기의 가짜 효과를, 검정력 25%로 간헐적으로 검출한 것**으로 정합적으로 설명된다.

⑧까지 교정하면(4.5절 C) 점추정치는 AUC 0.481, t=−1.49가 되어 양(+)의 효과 자체가 사라진다. 다만 t=−1.49 역시 0.5와 유의하게 다르지 않으므로, **효과가 정확히 0이라고 주장할 수는 없다.**

따라서 본 연구의 결론은 **"예측력이 없음을 증명했다"가 아니다.** 정확한 진술은 다음과 같다.

> 모든 결함을 교정한 뒤의 점추정치는 AUC 0.481로 무작위(0.5) 아래이며, 유의하지 않다. 앞선 분석들에서 관측되던 미약한 양(+)의 신호는 결함, 특히 ⑧에 의해 생성된 것이었다. 다만 현재 데이터의 검정력은 25% 수준이므로, **미약한 실제 신호의 존재 가능성 자체를 배제할 수는 없다.**

이는 ⑥(다중검정)과 결합될 때 특히 위험하다. 검정력이 25%인 상황에서 여러 설정을 시도하고 최선을 보고하면, 그 결과는 실제 신호의 발견이 아니라 **운 좋은 25%에 걸린 사례**일 가능성이 높다.

---

## 5. 논의

### 5.1 결함은 독립적으로 더해지지 않는다

4.1절의 결과를 기준선 대비 AUC 변화량으로 분해하면 다음과 같다.

| 결함 | 단독 효과 (ΔAUC) |
|---|---:|
| ① iid 가정 p값 | 0.000 (AUC 불변, p값만 변화) |
| ② 소표본 유니버스 | +0.024 |
| ③ 연간 룩어헤드 | −0.004 |
| ④ 성장률 부호 오류 | +0.004 |
| ⑤ 평가일 솎아내기 | −0.037 |
| **단순 합산** | **−0.013** |
| **모두 동시 적용 (실측)** | **+0.073** |

개별 효과를 더하면 오히려 음수이지만, 동시에 적용하면 +0.073이다. 효과가 **가산적이지 않다**.

> **해석상의 주의**: 본 연구는 완전요인설계(full factorial)를 수행하지 않았다. '모두 동시 적용' 조건은 기준선과 다섯 가지가 동시에 다르므로, 위 차이(+0.073 대 −0.013)를 특정 결함 쌍의 상호작용으로 귀속시킬 수는 없다. 엄밀한 상호작용 추정에는 2⁵ = 32개 조합의 실행이 필요하며, 이는 향후 과제로 남긴다. 다만 **효과가 단순 가산적이지 않다는 사실 자체**는 위 비교로 충분히 확인된다.

작동 구조에 대해서는 다음의 부분적 근거가 있다. ②는 AUC 추정치를 끌어올리지만(0.514 → 0.538) ①은 AUC를 전혀 바꾸지 못한다(4.2절). 즉 ①은 추론에만 작용하므로, **다른 결함이 추정치를 올려놓은 뒤에야 유의성으로 전환될 수 있다.** 실제로 ①을 단독 적용한 조건에서 p가 0.0000이 된 것은 기준선 AUC가 이미 0.5를 상회(0.514)했기 때문이며, 이 0.514 자체가 ⑧에 의해 부풀려진 값이다(4.5절).

이는 실무적으로 중요하다. **단일 결함만 점검하는 코드 리뷰나 감사로는 이러한 조합 효과를 탐지하기 어렵다.** 각 결함을 개별적으로 평가하면 "영향이 미미하다"는 결론에 도달하기 쉽기 때문이다.

### 5.2 제품 목표와 연구 목표의 충돌

본 시스템은 개인용 투자 대시보드이자 연구 도구였다. 두 목표의 요구사항은 상충한다.

| | 제품이 요구하는 것 | 연구가 요구하는 것 |
|---|---|---|
| 데이터 | 현재 시점의 정확한 값 | 당시에 알 수 있었던 값 |
| 유니버스 | 사용자의 관심 종목 | 편향 없는 표본 |
| 이상한 결과 | 버그 | 발견(finding) |

개발 과정에서 공시 지연 상수는 **화면 표시값이 상용 증권 앱과 일치하도록** 90일에서 45일로 조정되었다. 이는 제품 관점에서는 개선이었으나, 연구 관점에서는 룩어헤드 방지 장치를 훼손하는 변경이었다(3.3절). 하나의 설정값이 두 목표를 동시에 섬길 수 없다.

### 5.3 "가치투자 모델"이라는 명명의 문제

4.4절에서 보았듯 본 모델의 통계적 유의성은 재무 지표가 아니라 **가격 변동성**에서, 그것도 6개월이 아닌 **1개월** 구간에서 발생한다. 이는 모델이 실제로 포착하는 것이 가치 프리미엄이 아니라 단기 저변동성 이상현상일 가능성을 시사한다. 모델의 명칭과 실제 작동이 일치하지 않는 사례이며, 변수 중요도 분석(가격 변수 33.6%)도 이를 뒷받침한다.

---

## 6. 한계

1. **소급 수정(restatement) 문제** — yfinance가 제공하는 과거 재무는 '현재 시점에서 본 과거'이므로 당시 발표치와 다를 수 있다. 시점 고정(point-in-time) 재무 데이터는 무료로 확보할 수 없다.

2. **생존 편향의 부분적 교정** — 시점별 구성종목 데이터로 부분 교정을 시도하였으나, 지수에서 탈락한 종목의 약 **절반(114개 중 62개만 확보)은 인수·합병 또는 사명 변경으로 yfinance에서 조회되지 않는다.** 더욱이 **그 결측은 무작위가 아니다.** 인수된 기업은 통상 프리미엄을 받고 종료되므로, 누락된 표본은 한 방향으로 치우쳐 있다.

3. **데이터 제공처 간 불일치** — 동일 종목에 대해 데이터 제공처마다 TTM EPS가 다르다. 예컨대 특정 종목의 EPS는 yfinance −8.66, 상용 증권 앱 −7.98로 약 8.5% 차이를 보였다(BPS와 ROE는 일치). 이는 주식수 산정 시점 또는 희석 기준의 차이로 추정된다.

4. **검정의 낙관성** — Newey-West 보정을 적용해도 오탐률이 명목 5%가 아닌 **10%**로 남는다(4.2절 표 2). 유효 독립 정보량이 (데이터 기간 ÷ 예측 기간)에 제약되기 때문이다. 자유도로 날짜 수 − 1을 사용한 것은 관례를 따른 것이며, 다소 낙관적이다.

5. **단일 시장·단일 기간** — 미국 대형주, 최근 5년에 한정된다. 결함의 기여도는 유니버스 구성에 따라 달라질 수 있으며, 실제로 ④가 그 사례이다(4.6절).

6. **절제 실험의 기준선 조건부성** — 표 1의 ②~⑤ 단독 효과는 모두 **⑧이 교정되지 않은 기준선**에 대해 측정되었다. ⑧이 유니버스 구성 자체를 바꾸어 다른 결함들과 독립적으로 조작할 수 없었기 때문이다. 따라서 표 1의 수치는 "⑧이 존재하는 조건 하에서의 단독 효과"로 읽어야 하며, ⑧을 교정한 상태에서 각 결함의 효과는 달라질 수 있다.

7. **완전요인설계의 부재** — 5.1절에서 논한 바와 같이 결함 간 상호작용을 엄밀히 추정하려면 2⁵개 조합이 필요하나, 본 연구는 일변량 절제와 전체 조합 두 지점만 비교하였다.

---

## 7. 결론

본 연구는 새로운 예측 알고리즘을 제안하지 않는다. 대신 무료 공개 데이터로 구축한 실제 퀀트 파이프라인에서, 방법론적 결함이 각각 얼마만큼의 '없는 예측력'을 만들어내는지를 절제 실험으로 분해하였다.

주요 결과는 다음과 같다.

1. 결함을 모두 포함한 초기 구현은 AUC 0.587, p=0.043의 "유의한 예측력"을 보였으나, 결함을 교정하면 유의성이 소멸한다.
2. **지수 편입일 룩어헤드가 단일 결함 중 최대 효과**를 보였다(AUC +0.030). 시점별 구성종목을 반영하면 점추정치는 0.481로 무작위 아래가 되며, 앞선 분석의 미약한 양(+)의 신호가 이 결함의 산물이었음이 드러난다.
3. 횡단면 종속성을 무시한 유의성 검정은 참효과가 0인 데이터에서 **35.5%의 오탐률**을 보인다. 동일한 AUC·t값에 대해 p값만 0.161 → 0.0000으로 변한다.
4. 42개 종목 규모에서는 **무작위 유니버스의 46%가 p<0.05**를 산출한다. 이 규모의 백테스트에서 얻은 유의성은 사실상 정보를 담지 않는다.
5. 60개 설정을 시도한 뒤 최선만 보고하면 p값이 0.161 → 0.0115로 변한다. 유의성은 특정 예측 기간(1개월)과 특정 변수(변동성)에 집중되어 있다.
6. 결함의 효과는 **가산적이지 않다.** 개별 점검으로는 조합 효과를 탐지할 수 없다.
7. 반면 일부 결함(③④⑦)은 본 설정에서 유의한 영향이 없었다. 결함의 논리적 심각성과 실제 왜곡 정도는 일치하지 않는다.
8. 본 데이터의 검정력은 약 25%로, 결론은 "신호 없음의 증명"이 아니라 **"관측된 신호는 결함의 산물이며, 실제 신호의 존재 여부는 판별 불가"**이다.

실무적 함의는 명확하다. **수십 개 종목과 무료 데이터로 구성된 백테스트에서 얻은 "통계적으로 유의한 예측력"은, 그 자체로는 거의 아무런 증거가 되지 못한다.**

---

## 참고문헌

- Bailey, D. H., Borwein, J., López de Prado, M., & Zhu, Q. J. (2014). Pseudo-Mathematics and Financial Charlatanism: The Effects of Backtest Overfitting on Out-of-Sample Performance. *Notices of the American Mathematical Society*, 61(5), 458–471.
- Fama, E. F., & MacBeth, J. D. (1973). Risk, Return, and Equilibrium: Empirical Tests. *Journal of Political Economy*, 81(3), 607–636.
- Hanley, J. A., & McNeil, B. J. (1982). The Meaning and Use of the Area under a Receiver Operating Characteristic (ROC) Curve. *Radiology*, 143(1), 29–36.
- Harvey, C. R., Liu, Y., & Zhu, H. (2016). … and the Cross-Section of Expected Returns. *Review of Financial Studies*, 29(1), 5–68.
- Hou, K., Xue, C., & Zhang, L. (2020). Replicating Anomalies. *Review of Financial Studies*, 33(5), 2019–2133.
- Newey, W. K., & West, K. D. (1987). A Simple, Positive Semi-Definite, Heteroskedasticity and Autocorrelation Consistent Covariance Matrix. *Econometrica*, 55(3), 703–708.

---

## 부록 A. 재현 방법

### A.1 코드 및 데이터 공개

본 연구의 전체 코드와 실험 산출물은 다음에서 공개한다.

> **저장소**: `[TODO: GitHub URL 기재]`
> 라이선스: `[TODO]` · 커밋 해시: `[TODO]`

외부 데이터는 모두 공개 출처에서 자동으로 수집되며 별도 구매가 필요 없다.

| 데이터 | 출처 | 접근 방식 |
|---|---|---|
| 가격·재무 | Yahoo Finance | `yfinance` 라이브러리 |
| 시점별 S&P500 구성종목 | 공개 GitHub 데이터셋 (`fja05680/sp500`) | 스크립트가 자동 다운로드 후 로컬 캐시 |
| 현재 구성종목·편입일 | Wikipedia | 스크립트가 자동 수집 |

### A.2 스크립트 대응표

| 스크립트 | 대응 절 | 산출물 |
|---|---|---|
| `simulate_falsepositive.py` | 4.2 | `simulate_falsepositive_results.csv` |
| `stats_utils.py` | 3.1 | 검정 구현 + 자체 검증(`py stats_utils.py`) |
| `ablation.py` | 4.1 | `ablation_results.csv` |
| `experiment_universe.py` | 4.3 | `experiment_universe_results.csv` |
| `experiment_multipletesting.py` | 4.4 | `experiment_multipletesting_results.csv` |
| `experiment_survivorship.py` | 4.5 | `experiment_survivorship_results.csv` |
| `experiment_stocksplit.py` | 4.6 | `experiment_stocksplit_results.csv` |

### A.3 핵심 구현 발췌

유의성 검정의 핵심은 다음과 같다(`stats_utils.py`).

```python
def fama_macbeth_auc(y_true, scores, dates, min_rows_per_date=5, nw_lag=0):
    """날짜별 AUC를 구한 뒤 그 시계열을 t검정한다."""
    per_date = []
    for a, b in date_blocks:              # 날짜 오름차순 (NW는 순서가 중요)
        y_d = y_sorted[a:b]
        if (b - a) < min_rows_per_date or y_d.min() == y_d.max():
            continue
        per_date.append(fast_auc(y_d, s_sorted[a:b]))

    T = len(per_date)
    mean_auc = np.mean(per_date)
    if nw_lag > 0:
        se = newey_west_se(per_date, nw_lag)   # 겹침에 의한 자기상관 보정
    else:
        se = np.std(per_date, ddof=1) / np.sqrt(T)
    tstat = (mean_auc - 0.5) / se
    pval = 1.0 - t_dist.cdf(tstat, df=T - 1)
    return mean_auc, tstat, pval, T


def newey_west_se(x, lag):
    """Bartlett 커널 기반 자기상관 보정 표준오차 (Newey-West 1987)."""
    T = len(x); e = np.asarray(x) - np.mean(x)
    s = (e @ e) / T                                   # gamma_0
    for j in range(1, min(int(lag), T - 1) + 1):
        s += 2.0 * (1.0 - j / (lag + 1.0)) * ((e[j:] @ e[:-j]) / T)
    return np.sqrt(s / T)


def fast_auc(y_true, scores):
    """AUC = (양성의 순위합 − n_pos(n_pos+1)/2) / (n_pos × n_neg).
    동점은 평균 순위로 처리하여 sklearn roc_auc_score와 정확히 일치한다."""
    pos = np.asarray(y_true) > 0
    n_pos, n_neg = int(pos.sum()), int(len(pos) - pos.sum())
    if n_pos == 0 or n_neg == 0:
        return float("nan")
    r = rankdata(scores)
    return float((r[pos].sum() - n_pos * (n_pos + 1) / 2.0) / (n_pos * n_neg))
```

비교 대상인 기존 방식(관측치 독립 가정)은 다음과 같다.

```python
def hanley_mcneil_pvalue(auc, n_pos, n_neg):
    """⚠️ 관측치가 독립일 때만 유효. 횡단면 패널에 적용하면 표준오차가 과소평가된다."""
    q1 = auc / (2.0 - auc)
    q2 = 2.0 * auc**2 / (1.0 + auc)
    se = np.sqrt((auc * (1 - auc) + (n_pos - 1) * (q1 - auc**2)
                  + (n_neg - 1) * (q2 - auc**2)) / (n_pos * n_neg))
    return 1.0 - norm.cdf((auc - 0.5) / se)
```

### A.4 결함 토글

파이프라인 본체는 `core.py`에 있으며, 결함은 모두 함수 인자로 켜고 끌 수 있다.

| 인자 | 기본값(교정) | 결함 재현 |
|---|---|---|
| `pvalue_method` | `"fama_macbeth"` | `"iid"` |
| `eval_mode` | `"overlap_nw"` | `"nonoverlap_iid"` |
| `lag_quarterly` / `lag_annual` | 45 / 90 | 45 / 45 |
| `growth_formula` | `"abs_base"` | `"naive"` |

무작위 추출에는 고정 시드(42)를 사용하였고, 병렬 수집 결과는 티커 순으로 정렬하여 결합함으로써 실행 간 재현성을 확보하였다.
